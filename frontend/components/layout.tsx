// import AppShell from "@/components/AppShell";

// export default function WorkspaceLayout({
//   children,
// }: {
//   children: React.ReactNode;
// }) {
//   // Later, we can fetch the actual Workspace name here on the server
//   // and pass it down to the Client AppShell!
//   return <AppShell workspaceName="My Workspace">{children}</AppShell>;
// }
